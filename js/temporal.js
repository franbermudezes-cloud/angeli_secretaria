export function temporalData(text){const value=(text||"").toLowerCase(),data={};if(/\bmañana\b/.test(value)){const date=new Date();date.setDate(date.getDate()+1);data.scheduledDate=date.toISOString().slice(0,10)}else{const match=value.match(/\b(\d{2})\/(\d{2})\/(\d{4})\b/);if(match){const date=new Date(`${match[3]}-${match[2]}-${match[1]}T12:00:00`);if(!Number.isNaN(date.getTime()))data.scheduledDate=date.toISOString().slice(0,10)}}const time=value.match(/(?:a las\s+)([01]?\d|2[0-3])(?::([0-5]\d))?|\b([01]?\d|2[0-3]):([0-5]\d)\b/);if(time){const hour=time[1]||time[3],minute=time[2]||time[4]||"00";data.scheduledTime=`${hour.padStart(2,"0")}:${minute}`}return data}
