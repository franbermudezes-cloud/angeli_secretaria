import{temporalData}from"./temporal.js?v=0.21.2";

export function classify(text,images=[],files=[]){const value=(text||"").toLowerCase();if(/\b(recuérdame|recordar)\b/.test(value))return"reminder";if(/\b(hacer|comprar|preparar|enviar|revisar)\b/.test(value))return"task";if(/\b(llama|llamar|telefonea|telefonear|contacta|contactar)\b/.test(value))return"contact";const temporal=temporalData(text);if(temporal.scheduledDate&&temporal.scheduledTime)return"calendar";if(images.length)return"photo";if(files.length)return"file";return"note"}
export function typeLabel(type){return({note:"Nota",task:"Tarea",reminder:"Recordatorio",calendar:"Calendario",contact:"Contacto",file:"Archivo",photo:"Foto"})[type]||"Nota"}
export function spokenPhone(text){const words={cero:"0",uno:"1",dos:"2",tres:"3",cuatro:"4",cinco:"5",seis:"6",siete:"7",ocho:"8",nueve:"9"},matches=(text.toLowerCase().match(/(?:(?:cero|uno|dos|tres|cuatro|cinco|seis|siete|ocho|nueve)[\s,-]*){9,}/g)||[]);for(const match of matches){const number=(match.match(/cero|uno|dos|tres|cuatro|cinco|seis|siete|ocho|nueve/g)||[]).map(word=>words[word]).join("");if(/^[6-9]\d{8}$/.test(number))return number}return""}
export function contactQuery(text){const match=(text||"").match(/\b(?:llama(?:r)?|telefonea(?:r)?|contacta(?:r)?)\s+(?:a\s+|al\s+)?(.+?)(?:[.!?,;]|$)/i);if(!match)return"";const query=match[1].replace(/\b(?:mañana|hoy|luego|por favor)\b.*$/i,"").trim();return/[\d]/.test(query)?"":query}
export function actionData(text,type){const value=(text||"").toLowerCase(),data={},phone=value.match(/(?:\+34\s*)?([6-9](?:[\s-]?\d){8})\b/),number=phone?phone[1].replace(/[\s-]/g,""):spokenPhone(value);if(number)data.phone=number;if(type==="contact"&&!number){const query=contactQuery(text);if(query)data.contactQuery=query}if(["task","reminder","calendar"].includes(type))Object.assign(data,temporalData(text));return data}
